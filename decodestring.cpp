#include <iostream>
#include <vector>
#include <algorithm>
#include <string>
using namespace std;
int solve(string str, int index)
{
    if (index >= str.size() - 1)
    {
        return 0;
    }
    if (str[index] == 0)
    {
        
    }
}
int main()
{
    string str = "111061";
    return 0;
}